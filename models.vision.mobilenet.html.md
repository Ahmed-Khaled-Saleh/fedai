# MobilenetV3


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/mobilenets.py#L63"
target="_blank" style="float:right; font-size:smaller">source</a>

### SEModule

``` python

def SEModule(
    in_channels_num, reduction_ratio:int=4
):

```

*SE Module* Ref:
https://github.com/moskomule/senet.pytorch/blob/master/senet/se_module.py

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/mobilenets.py#L52"
target="_blank" style="float:right; font-size:smaller">source</a>

### H_swish

``` python

def H_swish(
    inplace:bool=True
):

```

*hard swish*

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/mobilenets.py#L41"
target="_blank" style="float:right; font-size:smaller">source</a>

### H_sigmoid

``` python

def H_sigmoid(
    inplace:bool=True
):

```

*hard sigmoid*

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/mobilenets.py#L90"
target="_blank" style="float:right; font-size:smaller">source</a>

### Bottleneck

``` python

def Bottleneck(
    in_channels_num, exp_size, out_channels_num, kernel_size, stride, use_SE, NL, BN_momentum
):

```

*The basic unit of MobileNetV3*

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/mobilenets.py#L159"
target="_blank" style="float:right; font-size:smaller">source</a>

### MobileNetV3Backbone

``` python

def MobileNetV3Backbone(
    mode:str='small', num_classes:int=1000, input_size:int=224, width_multiplier:float=1.0, dropout:float=0.2,
    BN_momentum:float=0.1, zero_gamma:bool=False
):

```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/mobilenets.py#L313"
target="_blank" style="float:right; font-size:smaller">source</a>

### MobileNetV3

``` python

def MobileNetV3(
    mode:str='small', num_classes:int=1000, input_size:int=224, width_multiplier:float=1.0, dropout:float=0.2
):

```

*Base class for all neural network modules.*

Your models should also subclass this class.

Modules can also contain other Modules, allowing them to be nested in a
tree structure. You can assign the submodules as regular attributes::

    import torch.nn as nn
    import torch.nn.functional as F

    class Model(nn.Module):
        def __init__(self) -> None:
            super().__init__()
            self.conv1 = nn.Conv2d(1, 20, 5)
            self.conv2 = nn.Conv2d(20, 20, 5)

        def forward(self, x):
            x = F.relu(self.conv1(x))
            return F.relu(self.conv2(x))

Submodules assigned in this way will be registered, and will also have
their parameters converted when you call :meth:`to`, etc.

.. note:: As per the example above, an `__init__()` call to the parent
class must be made before assignment on the child.

:ivar training: Boolean represents whether this module is in training or
evaluation mode. :vartype training: bool

## Pretrained MobilenetV3

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/mobilenets.py#L344"
target="_blank" style="float:right; font-size:smaller">source</a>

### PretrainedMobileNetv3

``` python

def PretrainedMobileNetv3(
    backbone, in_features, num_classes:int=10
):

```

*Base class for all neural network modules.*

Your models should also subclass this class.

Modules can also contain other Modules, allowing them to be nested in a
tree structure. You can assign the submodules as regular attributes::

    import torch.nn as nn
    import torch.nn.functional as F

    class Model(nn.Module):
        def __init__(self) -> None:
            super().__init__()
            self.conv1 = nn.Conv2d(1, 20, 5)
            self.conv2 = nn.Conv2d(20, 20, 5)

        def forward(self, x):
            x = F.relu(self.conv1(x))
            return F.relu(self.conv2(x))

Submodules assigned in this way will be registered, and will also have
their parameters converted when you call :meth:`to`, etc.

.. note:: As per the example above, an `__init__()` call to the parent
class must be made before assignment on the child.

:ivar training: Boolean represents whether this module is in training or
evaluation mode. :vartype training: bool

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/mobilenets.py#L359"
target="_blank" style="float:right; font-size:smaller">source</a>

### mobilenetv3x

``` python

def mobilenetv3x(
    x:str='small', num_classes:int=10
):

```

## A Unified API for mobilenetv3

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/mobilenets.py#L384"
target="_blank" style="float:right; font-size:smaller">source</a>

### make_mobilenetv3

``` python

def make_mobilenetv3(
    variant:Literal='small', pretrained:bool=False, num_classes:int=10
)->Module:

```

*Create a MobileNetV3 model.*

Args: variant: Model variant (‘small’ or ‘large’) num_classes: Number of
output classes pretrained: Whether to load pretrained weights

Examples: \>\>\> model = make_mobilenetv3(‘small’) \>\>\> model =
make_mobilenetv3(‘large’, num_classes=1000, pretrained=True)
