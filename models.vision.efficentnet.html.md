# EfficientNets


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

### Standard EfficientNets

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/efficientnets.py#L32"
target="_blank" style="float:right; font-size:smaller">source</a>

### SE

``` python

def SE(
    in_channels, se_channels
):

```

*Squeeze-and-Excitation block with Swish.*

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/efficientnets.py#L23"
target="_blank" style="float:right; font-size:smaller">source</a>

### drop_connect

``` python

def drop_connect(
    x, drop_ratio
):

```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/efficientnets.py#L19"
target="_blank" style="float:right; font-size:smaller">source</a>

### swish

``` python

def swish(
    x
):

```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/efficientnets.py#L50"
target="_blank" style="float:right; font-size:smaller">source</a>

### Block

``` python

def Block(
    in_channels, out_channels, kernel_size, stride, expand_ratio:int=1, se_ratio:float=0.0, drop_rate:float=0.0
):

```

*expansion + depthwise + pointwise + squeeze-excitation*

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/efficientnets.py#L131"
target="_blank" style="float:right; font-size:smaller">source</a>

### EfficientNetBackbone

``` python

def EfficientNetBackbone(
    width_mult:float=1.0, depth_mult:float=1.0, num_classes:int=10
):

```

*Base class for all neural network modules.*

Your models should also subclass this class.

Modules can also contain other Modules, allowing them to be nested in a
tree structure. You can assign the submodules as regular attributes::

    import torch.nn as nn
    import torch.nn.functional as F

    class Model(nn.Module):
        def __init__(self) -> None:
            super().__init__()
            self.conv1 = nn.Conv2d(1, 20, 5)
            self.conv2 = nn.Conv2d(20, 20, 5)

        def forward(self, x):
            x = F.relu(self.conv1(x))
            return F.relu(self.conv2(x))

Submodules assigned in this way will be registered, and will also have
their parameters converted when you call :meth:`to`, etc.

.. note:: As per the example above, an `__init__()` call to the parent
class must be made before assignment on the child.

:ivar training: Boolean represents whether this module is in training or
evaluation mode. :vartype training: bool

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/efficientnets.py#L126"
target="_blank" style="float:right; font-size:smaller">source</a>

### round_repeats

``` python

def round_repeats(
    repeats, depth_mult
):

```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/efficientnets.py#L117"
target="_blank" style="float:right; font-size:smaller">source</a>

### round_filters

``` python

def round_filters(
    filters, width_mult
):

```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/efficientnets.py#L184"
target="_blank" style="float:right; font-size:smaller">source</a>

### EfficientNet

``` python

def EfficientNet(
    width_mult:float=1.0, depth_mult:float=1.0, dropout_rate:float=0.2, num_classes:int=10
):

```

*Base class for all neural network modules.*

Your models should also subclass this class.

Modules can also contain other Modules, allowing them to be nested in a
tree structure. You can assign the submodules as regular attributes::

    import torch.nn as nn
    import torch.nn.functional as F

    class Model(nn.Module):
        def __init__(self) -> None:
            super().__init__()
            self.conv1 = nn.Conv2d(1, 20, 5)
            self.conv2 = nn.Conv2d(20, 20, 5)

        def forward(self, x):
            x = F.relu(self.conv1(x))
            return F.relu(self.conv2(x))

Submodules assigned in this way will be registered, and will also have
their parameters converted when you call :meth:`to`, etc.

.. note:: As per the example above, an `__init__()` call to the parent
class must be made before assignment on the child.

:ivar training: Boolean represents whether this module is in training or
evaluation mode. :vartype training: bool

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/efficientnets.py#L224"
target="_blank" style="float:right; font-size:smaller">source</a>

### EfficientNetB7

``` python

def EfficientNetB7(
    num_classes:int=10
):

```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/efficientnets.py#L221"
target="_blank" style="float:right; font-size:smaller">source</a>

### EfficientNetB6

``` python

def EfficientNetB6(
    num_classes:int=10
):

```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/efficientnets.py#L218"
target="_blank" style="float:right; font-size:smaller">source</a>

### EfficientNetB5

``` python

def EfficientNetB5(
    num_classes:int=10
):

```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/efficientnets.py#L215"
target="_blank" style="float:right; font-size:smaller">source</a>

### EfficientNetB4

``` python

def EfficientNetB4(
    num_classes:int=10
):

```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/efficientnets.py#L212"
target="_blank" style="float:right; font-size:smaller">source</a>

### EfficientNetB3

``` python

def EfficientNetB3(
    num_classes:int=10
):

```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/efficientnets.py#L209"
target="_blank" style="float:right; font-size:smaller">source</a>

### EfficientNetB2

``` python

def EfficientNetB2(
    num_classes:int=10
):

```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/efficientnets.py#L206"
target="_blank" style="float:right; font-size:smaller">source</a>

### EfficientNetB1

``` python

def EfficientNetB1(
    num_classes:int=10
):

```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/efficientnets.py#L203"
target="_blank" style="float:right; font-size:smaller">source</a>

### EfficientNetB0

``` python

def EfficientNetB0(
    num_classes:int=10
):

```

### Pretrained EfficientNet

In case you use any of the pretrained resnets, we will need to
`upsample` the input images to `224x224` as the pretrained resnets are
trained on imagenet which has images of size `224x224`.

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/efficientnets.py#L232"
target="_blank" style="float:right; font-size:smaller">source</a>

### PretrainedEfficient

``` python

def PretrainedEfficient(
    backbone, in_features, num_classes:int=10
):

```

*Base class for all neural network modules.*

Your models should also subclass this class.

Modules can also contain other Modules, allowing them to be nested in a
tree structure. You can assign the submodules as regular attributes::

    import torch.nn as nn
    import torch.nn.functional as F

    class Model(nn.Module):
        def __init__(self) -> None:
            super().__init__()
            self.conv1 = nn.Conv2d(1, 20, 5)
            self.conv2 = nn.Conv2d(20, 20, 5)

        def forward(self, x):
            x = F.relu(self.conv1(x))
            return F.relu(self.conv2(x))

Submodules assigned in this way will be registered, and will also have
their parameters converted when you call :meth:`to`, etc.

.. note:: As per the example above, an `__init__()` call to the parent
class must be made before assignment on the child.

:ivar training: Boolean represents whether this module is in training or
evaluation mode. :vartype training: bool

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/efficientnets.py#L247"
target="_blank" style="float:right; font-size:smaller">source</a>

### efficientx

``` python

def efficientx(
    x:int=0, num_classes:int=1000
):

```

## Unified API for EfficientNets

The API allows to create different variants of EfficientNets with or
without pretrained weights using a single function `create_model`. For
example:

``` python
 model = make_efficientnet(0)  # EfficientNet-B0
 model = make_efficientnet('b3')  # EfficientNet-B3
 model = make_efficientnet('efficientnet_b5')  # EfficientNet-B5
```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/efficientnets.py#L279"
target="_blank" style="float:right; font-size:smaller">source</a>

### make_efficientnet

``` python

def make_efficientnet(
    variant:Union=0, pretrained:bool=False, num_classes:int=10
)->Module:

```

*Create an EfficientNet model.*

Args: variant: Model variant, either int (0-7) or string (‘b0’-‘b7’,
‘efficientnet_b0’-‘efficientnet_b7’) num_classes: Number of output
classes pretrained: Whether to load pretrained weights

Examples: \>\>\> model = make_efficientnet(0) \# EfficientNet-B0 \>\>\>
model = make_efficientnet(‘b3’) \# EfficientNet-B3 \>\>\> model =
make_efficientnet(‘efficientnet_b5’) \# EfficientNet-B5
