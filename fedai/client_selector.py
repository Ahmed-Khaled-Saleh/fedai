"""Implement your client selection Algorithms here."""

# AUTOGENERATED! DO NOT EDIT! File to edit: ../nbs/10_client_selection.ipynb.

# %% auto 0
__all__ = ['BaseClientSelector']

# %% ../nbs/10_client_selection.ipynb 3
import numpy as np
import random
import torch

# %% ../nbs/10_client_selection.ipynb 4
class BaseClientSelector:
    def __init__(self, cfg):
        self.cfg = cfg

    def select(self):
        torch.manual_seed(self.cfg.random_seed)  
        np.random.seed(self.cfg.random_seed)
        random.seed(self.cfg.random_seed)
        
        active_clients_ids = []
        for _ in range(self.cfg.n_rounds):
            active_clients_ids.append(np.random.choice(a= np.arange(self.cfg.num_clients), 
                                                        size=int(self.cfg.num_clients * self.cfg.m), 
                                                        replace=False))
            
        return active_clients_ids
