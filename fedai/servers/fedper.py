"""The core abstraction for `FedPer` server: https://arxiv.org/abs/1912.00818"""

# AUTOGENERATED! DO NOT EDIT! File to edit: ../../nbs/11h_server.fedper.ipynb.

# %% auto #0
__all__ = ['ServerFedPer']

# %% ../../nbs/11h_server.fedper.ipynb #95d57626
from fastcore.utils import *
from fastcore.all import *
import os

import copy
from tqdm import tqdm
from loguru import logger

import numpy as np
import pandas as pd

import torch
import torch.nn.functional as F
from torch.utils.data import DataLoader

from ..client_selector import BaseClientSelector
from ..data import prepare_dl
from ..models import create_model
from ..optimizers import get_optimizer

from .base_server import BaseServer
from ..utils.registery import AlgorithmRegistry
from ..core import get_clean_kwargs
from ..optimizers.custom_optimizers import PerturbedGradientDescent

# %% ../../nbs/11h_server.fedper.ipynb #c4456ff3
@AlgorithmRegistry.register_server("fedper")
class ServerFedPer(BaseServer):
    def __init__(self,
                 cfg,
                 client_selector,
                 client_cls,
                 criterion,
                 fds,
                 writer= None,
                 device= None,
                 **kwargs
                 ):  
                 
        super().__init__(cfg, client_selector, client_cls, criterion, fds, writer, device, **kwargs) 
                 

# %% ../../nbs/11h_server.fedper.ipynb #ce0e22b2
@patch
def aggregate(self: ServerFedPer, lst_active_ids, comm_round, len_clients_ds):
    m_t = sum(len_clients_ds.values())
    
    with torch.no_grad():
        global_model = None
        
        for id in lst_active_ids:
            client_state_dict = self.state_mgr.get_state(id).get('model', None)

            if global_model is None:
                global_model = {k: torch.zeros_like(v) for k, v in client_state_dict.items()}

            n_k = len_clients_ds[id]
            weight = n_k / m_t
            for key in global_model.keys():
                if key.startswith("head"):
                    global_model[key].add_(client_state_dict[key], alpha=weight)

        self.model.load_state_dict(global_model)

        for id in lst_active_ids:
            self.state_mgr.set_state(id, {
                    'model': self.model.state_dict(),
                    'optimizer': self.state_mgr.get_state(id).get('optimizer', None),
            })
        
