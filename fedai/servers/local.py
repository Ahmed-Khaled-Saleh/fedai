"""The core abstraction for `local` server."""

# AUTOGENERATED! DO NOT EDIT! File to edit: ../../nbs/11g_server.local.ipynb.

# %% auto #0
__all__ = ['ServerLocal']

# %% ../../nbs/11g_server.local.ipynb #95d57626
from fastcore.utils import *
from fastcore.all import *
import os

from tqdm import tqdm
from loguru import logger

import numpy as np
import pandas as pd

import torch
import torch.nn.functional as F
from torch.utils.data import DataLoader

from .base_server import BaseServer
from ..utils.registery import AlgorithmRegistry

# %% ../../nbs/11g_server.local.ipynb #c4456ff3
@AlgorithmRegistry.register_server("local")
class ServerLocal(BaseServer):
    def __init__(self,
                 cfg,
                 client_selector,
                 client_cls,
                 criterion,
                 fds,
                 writer= None,
                 device= None,
                 **kwargs
                 ):  
                 
        super().__init__(cfg, client_selector, client_cls, criterion, fds, writer, device, **kwargs) 
                 

# %% ../../nbs/11g_server.local.ipynb #ce0e22b2
@patch
def aggregate(self: ServerLocal, lst_active_ids, comm_round, len_clients_ds):
    pass
        
