# ResNets


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

### Standard Resnet

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/resnets.py#L19"
target="_blank" style="float:right; font-size:smaller">source</a>

### BasicBlock

``` python

def BasicBlock(
    in_planes, planes, stride:int=1
):

```

*Base class for all neural network modules.*

Your models should also subclass this class.

Modules can also contain other Modules, allowing them to be nested in a
tree structure. You can assign the submodules as regular attributes::

    import torch.nn as nn
    import torch.nn.functional as F

    class Model(nn.Module):
        def __init__(self) -> None:
            super().__init__()
            self.conv1 = nn.Conv2d(1, 20, 5)
            self.conv2 = nn.Conv2d(20, 20, 5)

        def forward(self, x):
            x = F.relu(self.conv1(x))
            return F.relu(self.conv2(x))

Submodules assigned in this way will be registered, and will also have
their parameters converted when you call :meth:`to`, etc.

.. note:: As per the example above, an `__init__()` call to the parent
class must be made before assignment on the child.

:ivar training: Boolean represents whether this module is in training or
evaluation mode. :vartype training: bool

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/mobilenets.py#L90"
target="_blank" style="float:right; font-size:smaller">source</a>

### Bottleneck

``` python

def Bottleneck(
    in_planes, planes, stride:int=1
):

```

*Base class for all neural network modules.*

Your models should also subclass this class.

Modules can also contain other Modules, allowing them to be nested in a
tree structure. You can assign the submodules as regular attributes::

    import torch.nn as nn
    import torch.nn.functional as F

    class Model(nn.Module):
        def __init__(self) -> None:
            super().__init__()
            self.conv1 = nn.Conv2d(1, 20, 5)
            self.conv2 = nn.Conv2d(20, 20, 5)

        def forward(self, x):
            x = F.relu(self.conv1(x))
            return F.relu(self.conv2(x))

Submodules assigned in this way will be registered, and will also have
their parameters converted when you call :meth:`to`, etc.

.. note:: As per the example above, an `__init__()` call to the parent
class must be made before assignment on the child.

:ivar training: Boolean represents whether this module is in training or
evaluation mode. :vartype training: bool

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/resnets.py#L105"
target="_blank" style="float:right; font-size:smaller">source</a>

### ResNet

``` python

def ResNet(
    block, num_blocks, num_classes:int=10
):

```

*Base class for all neural network modules.*

Your models should also subclass this class.

Modules can also contain other Modules, allowing them to be nested in a
tree structure. You can assign the submodules as regular attributes::

    import torch.nn as nn
    import torch.nn.functional as F

    class Model(nn.Module):
        def __init__(self) -> None:
            super().__init__()
            self.conv1 = nn.Conv2d(1, 20, 5)
            self.conv2 = nn.Conv2d(20, 20, 5)

        def forward(self, x):
            x = F.relu(self.conv1(x))
            return F.relu(self.conv2(x))

Submodules assigned in this way will be registered, and will also have
their parameters converted when you call :meth:`to`, etc.

.. note:: As per the example above, an `__init__()` call to the parent
class must be made before assignment on the child.

:ivar training: Boolean represents whether this module is in training or
evaluation mode. :vartype training: bool

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/resnets.py#L73"
target="_blank" style="float:right; font-size:smaller">source</a>

### ResNetBackbone

``` python

def ResNetBackbone(
    block, num_blocks
):

```

*Base class for all neural network modules.*

Your models should also subclass this class.

Modules can also contain other Modules, allowing them to be nested in a
tree structure. You can assign the submodules as regular attributes::

    import torch.nn as nn
    import torch.nn.functional as F

    class Model(nn.Module):
        def __init__(self) -> None:
            super().__init__()
            self.conv1 = nn.Conv2d(1, 20, 5)
            self.conv2 = nn.Conv2d(20, 20, 5)

        def forward(self, x):
            x = F.relu(self.conv1(x))
            return F.relu(self.conv2(x))

Submodules assigned in this way will be registered, and will also have
their parameters converted when you call :meth:`to`, etc.

.. note:: As per the example above, an `__init__()` call to the parent
class must be made before assignment on the child.

:ivar training: Boolean represents whether this module is in training or
evaluation mode. :vartype training: bool

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/resnets.py#L130"
target="_blank" style="float:right; font-size:smaller">source</a>

### ResNet152

``` python

def ResNet152(
    num_classes:int=10
):

```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/resnets.py#L127"
target="_blank" style="float:right; font-size:smaller">source</a>

### ResNet101

``` python

def ResNet101(
    num_classes:int=10
):

```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/resnets.py#L124"
target="_blank" style="float:right; font-size:smaller">source</a>

### ResNet50

``` python

def ResNet50(
    num_classes:int=10
):

```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/resnets.py#L121"
target="_blank" style="float:right; font-size:smaller">source</a>

### ResNet34

``` python

def ResNet34(
    num_classes:int=10
):

```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/resnets.py#L118"
target="_blank" style="float:right; font-size:smaller">source</a>

### ResNet18

``` python

def ResNet18(
    num_classes:int=10
):

```

### Pre-Activation Resnets

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/resnets.py#L135"
target="_blank" style="float:right; font-size:smaller">source</a>

### PreActBlock

``` python

def PreActBlock(
    in_planes, planes, stride:int=1
):

```

*Pre-activation version of the BasicBlock.*

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/resnets.py#L160"
target="_blank" style="float:right; font-size:smaller">source</a>

### PreActBottleneck

``` python

def PreActBottleneck(
    in_planes, planes, stride:int=1
):

```

*Pre-activation version of the original Bottleneck module.*

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/resnets.py#L225"
target="_blank" style="float:right; font-size:smaller">source</a>

### PreActResNet

``` python

def PreActResNet(
    block, num_blocks, num_classes:int=10
):

```

*Base class for all neural network modules.*

Your models should also subclass this class.

Modules can also contain other Modules, allowing them to be nested in a
tree structure. You can assign the submodules as regular attributes::

    import torch.nn as nn
    import torch.nn.functional as F

    class Model(nn.Module):
        def __init__(self) -> None:
            super().__init__()
            self.conv1 = nn.Conv2d(1, 20, 5)
            self.conv2 = nn.Conv2d(20, 20, 5)

        def forward(self, x):
            x = F.relu(self.conv1(x))
            return F.relu(self.conv2(x))

Submodules assigned in this way will be registered, and will also have
their parameters converted when you call :meth:`to`, etc.

.. note:: As per the example above, an `__init__()` call to the parent
class must be made before assignment on the child.

:ivar training: Boolean represents whether this module is in training or
evaluation mode. :vartype training: bool

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/resnets.py#L188"
target="_blank" style="float:right; font-size:smaller">source</a>

### PreActResNetBacbone

``` python

def PreActResNetBacbone(
    block, num_blocks
):

```

*Base class for all neural network modules.*

Your models should also subclass this class.

Modules can also contain other Modules, allowing them to be nested in a
tree structure. You can assign the submodules as regular attributes::

    import torch.nn as nn
    import torch.nn.functional as F

    class Model(nn.Module):
        def __init__(self) -> None:
            super().__init__()
            self.conv1 = nn.Conv2d(1, 20, 5)
            self.conv2 = nn.Conv2d(20, 20, 5)

        def forward(self, x):
            x = F.relu(self.conv1(x))
            return F.relu(self.conv2(x))

Submodules assigned in this way will be registered, and will also have
their parameters converted when you call :meth:`to`, etc.

.. note:: As per the example above, an `__init__()` call to the parent
class must be made before assignment on the child.

:ivar training: Boolean represents whether this module is in training or
evaluation mode. :vartype training: bool

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/resnets.py#L250"
target="_blank" style="float:right; font-size:smaller">source</a>

### PreActResNet152

``` python

def PreActResNet152(
    num_classes:int=10
):

```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/resnets.py#L247"
target="_blank" style="float:right; font-size:smaller">source</a>

### PreActResNet101

``` python

def PreActResNet101(
    num_classes:int=10
):

```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/resnets.py#L244"
target="_blank" style="float:right; font-size:smaller">source</a>

### PreActResNet50

``` python

def PreActResNet50(
    num_classes:int=10
):

```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/resnets.py#L241"
target="_blank" style="float:right; font-size:smaller">source</a>

### PreActResNet34

``` python

def PreActResNet34(
    num_classes:int=10
):

```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/resnets.py#L238"
target="_blank" style="float:right; font-size:smaller">source</a>

### PreActResNet18

``` python

def PreActResNet18(
    num_classes:int=10
):

```

### Pretrained Resnets

In case you use any of the pretrained resnets, we will need to
`upsample` the input images to `224x224` as the pretrained resnets are
trained on imagenet which has images of size `224x224`.

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/resnets.py#L259"
target="_blank" style="float:right; font-size:smaller">source</a>

### PretrainedResNet

``` python

def PretrainedResNet(
    backbone, in_features, num_classes:int=10
):

```

*Base class for all neural network modules.*

Your models should also subclass this class.

Modules can also contain other Modules, allowing them to be nested in a
tree structure. You can assign the submodules as regular attributes::

    import torch.nn as nn
    import torch.nn.functional as F

    class Model(nn.Module):
        def __init__(self) -> None:
            super().__init__()
            self.conv1 = nn.Conv2d(1, 20, 5)
            self.conv2 = nn.Conv2d(20, 20, 5)

        def forward(self, x):
            x = F.relu(self.conv1(x))
            return F.relu(self.conv2(x))

Submodules assigned in this way will be registered, and will also have
their parameters converted when you call :meth:`to`, etc.

.. note:: As per the example above, an `__init__()` call to the parent
class must be made before assignment on the child.

:ivar training: Boolean represents whether this module is in training or
evaluation mode. :vartype training: bool

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/resnets.py#L274"
target="_blank" style="float:right; font-size:smaller">source</a>

### resnetx

``` python

def resnetx(
    x:int=18, num_classes:int=1000
):

```

## Unifrom API for Resnets

We will create a uniform API for creating resnets. This will allow us to
easily switch between different resnet variants and pretrained vs from
scratch models. The API will be as follows:

``` python
model = create_model('resnet_50', num_classes=1000, pretrained=True)
model = create_model('resnet_50', num_classes=10, pretrained=False)
model = create_model('resnet_50.preact', num_classes=10, pretrained=False)
```

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/models/vision/resnets.py#L303"
target="_blank" style="float:right; font-size:smaller">source</a>

### make_resnet

``` python

def make_resnet(
    variant:Union=18, pretrained:bool=False, num_classes:int=10, preactivation:bool=False
)->Module:

```
