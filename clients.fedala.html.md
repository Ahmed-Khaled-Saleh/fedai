# FedALA Client


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

### ALA class

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/clients/fedala.py#L25"
target="_blank" style="float:right; font-size:smaller">source</a>

### ALA

``` python

def ALA(
    cid:int, criterion:Module, train_data:List, batch_size:int, rand_percent:int, data_key:str, label_key:str,
    layer_idx:int=0, eta:float=1.0, device:str='cpu', threshold:float=0.1, num_pre_loss:int=10,
    start_phase:bool=True, weights:Tensor=None
)->None:

```

*Initialize ALA module*

Args: cid: Client ID. criterion: The loss function. train_data: The
reference of the local training data. batch_size: Weight learning batch
size. rand_percent: The percent of the local training data to sample.
layer_idx: Control the weight range. By default, all the layers are
selected. Default: 0 eta: Weight learning rate. Default: 1.0 device:
Using cuda or cpu. Default: ‘cpu’ threshold: Train the weight until the
standard deviation of the recorded losses is less than a given
threshold. Default: 0.1 num_pre_loss: The number of the recorded losses
to be considered to calculate the standard deviation. Default: 10
start_phase: Whether to start the phase of adaptive local aggregation.
Default: True weights: The initial weights for adaptive local
aggregation. Default: None Returns: None.

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/clients/fedala.py#L80"
target="_blank" style="float:right; font-size:smaller">source</a>

### ALA.adaptive_local_aggregation

``` python

def adaptive_local_aggregation(
    global_model:Module, local_model:Module
)->None:

```

*Generates the Dataloader for the randomly sampled local training data
and * preserves the lower layers of the update.

Args: global_model: The received global/aggregated model. local_model:
The trained local model.

Returns: None.

## ClientDitto

This class implements the basic abstraction of a client and is
considered the base class for all type of clients

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/clients/fedala.py#L190"
target="_blank" style="float:right; font-size:smaller">source</a>

### ClientFedALA

``` python

def ClientFedALA(
    id, # Unique identifier for the client
    cfg, # A configuration object containing all the necessary parameters for training and evaluation.
    train_loader, test_loader,
    state, # A dictionary containing the model, optimizer and any changing component needed.
    criterion, device:NoneType=None, t:int=0, kwargs:VAR_KEYWORD
):

```

*Initialize self. See help(type(self)) for accurate signature.*

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/clients/fedala.py#L220"
target="_blank" style="float:right; font-size:smaller">source</a>

### ClientFedALA.local_initialization

``` python

def local_initialization(
    received_global_model
):

```

### Training

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/clients/apfl.py#L52"
target="_blank" style="float:right; font-size:smaller">source</a>

### fit

``` python

def fit(
    
):

```

### Saving

------------------------------------------------------------------------

<a
href="https://github.com/Ahmed-Khaled-Saleh/fedai/blob/main/fedai/clients/fedala.py#L241"
target="_blank" style="float:right; font-size:smaller">source</a>

### ClientFedALA.save_state

``` python

def save_state(
    save_to_disk:bool=False
):

```
